import { AccountInfoRequest } from './account-info-request';

describe('AccountInfoRequest', () => {
  it('should create an instance', () => {
    expect(new AccountInfoRequest()).toBeTruthy();
  });
});
