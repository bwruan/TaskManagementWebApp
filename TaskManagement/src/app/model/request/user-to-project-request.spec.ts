import { UserToProjectRequest } from './user-to-project-request';

describe('UserToProjectRequest', () => {
  it('should create an instance', () => {
    expect(new UserToProjectRequest()).toBeTruthy();
  });
});
