import { TaskCommentService } from './task-comment-service';

describe('TaskCommentService', () => {
  it('should create an instance', () => {
    expect(new TaskCommentService()).toBeTruthy();
  });
});
